# 나누어 떨어지는 숫자 배열

```python
def solution(arr, divisor):
    answer = []
    
    for a in arr:
        if a % divisor == 0:
            answer.append(a)
    
    if answer:
        answer.sort()
    else:
        answer = [-1]
    
    return answer
```

