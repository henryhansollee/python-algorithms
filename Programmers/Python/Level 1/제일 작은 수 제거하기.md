# 제일 작은 수 제거하기

```python
def solution(arr):
    answer = []
    if len(arr) == 1:
        answer = [-1]
    else:
        min_num = min(arr)
        min_idx = arr.index(min_num)
        arr.pop(min_idx)
        answer = arr
    return answer
```

