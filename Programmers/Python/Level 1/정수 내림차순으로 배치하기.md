# 정수 내림차순으로 배치하기

```python
def solution(n):
    answer = 0
    n = str(n)
    nums = list(map(str, n))
    nums.sort()
    nums.reverse()
    answer = ''.join(nums)
    answer = int(answer)
    return answer
```

