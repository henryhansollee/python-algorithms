# 문자열 다루기 기본

```python
def solution(s):
    answer = True
    
    if len(s) != 4 and len(s) != 6:
        answer = False
    
    for i in range(len(s)):
        if not s[i].isdigit():
            answer = False
            break
        
    return answer
```

