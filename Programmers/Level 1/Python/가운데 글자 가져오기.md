# 가운데 글자 가져오기

```python
def solution(s):
    answer = ''
    n = len(s)
    idx = n // 2
    
    if n % 2 == 1:
        answer = s[idx]
    else:
        answer = s[idx-1] + s[idx]
    
    return answer
```

