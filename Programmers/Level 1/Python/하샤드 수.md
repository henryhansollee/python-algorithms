# 하샤드 수

```python
def solution(x):
    answer = True
    str_x = str(x)
    sum_x = 0
    
    for el_x in str_x:
        sum_x += int(el_x)
    
    if x % sum_x != 0:
        answer = False
    
    return answer
```

