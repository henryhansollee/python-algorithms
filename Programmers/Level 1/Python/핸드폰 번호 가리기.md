# 핸드폰 번호 가리기

```python
def solution(phone_number):
    answer = ''
    nums = list(map(str, phone_number))
    k = 4
    for i in range(len(nums)-1, -1, -1):
        if k > 0:
            k -= 1
        else:
            nums[i] = '*'
    answer = ''.join(nums)
    return answer
```

