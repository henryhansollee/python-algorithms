# 폰켓몬

```python
def solution(nums):
    answer = 0
    
    n = len(nums) // 2    
    new_nums = list(set(nums))
    
    for num in new_nums:
        if (answer < n):
            answer += 1

    return answer
```

