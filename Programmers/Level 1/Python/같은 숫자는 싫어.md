# 같은 숫자는 싫어

```python
def solution(arr):
    answer = []
    n = len(arr)
    
    for i in range(n):
        if not answer:
            answer.append(arr[i])
        else:
            if answer[-1] != arr[i]:
                answer.append(arr[i])
    
    return answer
```

